#ifndef HANDLE_H
#define HANDLE_H
/*
template <typename T> Handle {
	private:
		unsigned int index;
		unsigned int valid;
	public:
		unsigned int getIndex() {
			return index;
		}
		unsigned int isValid() {
			return valid;
		}
	Handle(unsigned int i, unsigned int v) {
		index = i;
		valid = v;
	}
	~Handle();
};*/

#endif // HANDLE_H